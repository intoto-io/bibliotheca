import { create } from '@storybook/theming';

import logo from './images/intoto-logo.svg';

export default create({
  base: 'dark',
  colorPrimary: '#06f',
  brandTitle: 'Intoto Studio',
  brandImage: logo,
});
